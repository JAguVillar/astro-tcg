---
import Layout from "../../layouts/Layout.astro";

const { id } = Astro.params;
const carta = id;
---

<Layout title="card">
  <picture> <img src={} transition:name=`carta ${id} box` /></picture>
</Layout>
